#pragma once
#include "Adafruit_GFX.h"
// Font descriptor stand-in: only the metrics the layout code relies on matter
// on the host (advance width and line height); the pixels come from render.py.
extern const GFXfont FreeSans9pt7b;
